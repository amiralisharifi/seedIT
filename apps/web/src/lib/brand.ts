// Single source for the public site's brand contact strings. Longer term
// these belong in a shared brand/site config (see AGENTS.md rule #1) so a
// fork overrides them in one place; until then, pages must import from here
// rather than inlining copies.
export const CONTACT_EMAIL = 'ceo@seedit.ae';
export const PHONE = '+971 54 443 5527';
export const WHATSAPP = 'https://wa.me/971544435527';
export const LICENCE = '1112101';
